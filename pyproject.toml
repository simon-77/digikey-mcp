[project]
name = "digikey-mcp"
version = "0.2.0"
description = "DigiKey MCP Server for product search, ordering, and order status"
dependencies = [
    "fastmcp",
    "requests",
    "python-dotenv",
]
requires-python = ">=3.10" 