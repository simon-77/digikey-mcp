[project]
name = "digikey-mcp"
version = "0.2.0"
description = "DigiKey component search, pricing, ordering, and order status"
license = "MIT"
dependencies = [
    "fastmcp",
    "requests",
    "python-dotenv",
]
requires-python = ">=3.10" 